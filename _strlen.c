#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - Function that returns the length of a string.
 * @s: char.
 * Return: strlen *s.
 */
int _strlen(char *s)
{

return (strlen(s));
}
